# Domain feature modules

- `auth/` — Phase 3 authentication and session security (implemented)
- Remaining domain modules are deferred per roadmap (users, officers, shifts, …)

Do not add empty Nest module stubs solely to mirror the folder tree.
